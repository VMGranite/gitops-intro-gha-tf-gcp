See [../task/README.md](../task/README.md) for the exercise. This is a
complete, working implementation — try the exercise yourself first.

`solution/infra/` and `solution/workflows/terraform.yml` are copied
into place at the repo root the same way `task/`'s are (see the task
README's Tasks step 1) — just point the `cp` commands at `solution/`
instead of `task/`.
