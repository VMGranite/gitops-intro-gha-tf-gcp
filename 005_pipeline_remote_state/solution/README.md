See [../task/README.md](../task/README.md) for the exercise. This is a
complete, working implementation — try the exercise yourself first.

`solution/infra/` and `solution/.github/` are copied into place at the
repo root the same way `task/`'s are (see the task README's Tasks step
1) — just point the `cp` commands at `solution/` instead of `task/`.
`workload_identity_provider` and `service_account` below are
placeholders — substitute your own values from
[003_gcp_github_trust](../../003_gcp_github_trust) Part 5.
