See [../task/README.md](../task/README.md) for the exercise. This is a
complete, working implementation — try the exercise yourself first.

`solution/.github/` is copied into place at the repo root the same way
`task/`'s is (see the task README's Tasks step 1). `infra/` is
unchanged from 005, so there's nothing to copy for it here.
`workload_identity_provider` and `service_account` are placeholders —
substitute your own values from
[003_gcp_github_trust](../../003_gcp_github_trust) Part 5.
